// Fotos rotativas do login (painel direito). O conteúdo vem de
// `login-fotos.generated.json`, gerado a partir de `static/login-fotos/`
// por `scripts/gen-login.mjs` no build/dev.
import gerado from './login-fotos.generated.json';

export const LOGIN_FOTOS: string[] = gerado as string[];
